alias brew-upgrade='brew update && brew upgrade' 
alias brew-cask-upgrade='brew cu' 
alias brew-cask-upgrade-force='brew cu --all' 
alias pip-upgrade="pip freeze | grep -v '^\-e' | cut -d = -f 1 | xargs -n1 pip install -U && pip freeze > ~/.dotfiles/pip-requirements.txt"
